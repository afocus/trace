run:
  timeout: 5m

linters:
  disable:
    - errcheck
  enable:
    - bodyclose
    - gocyclo
    - exportloopref

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0